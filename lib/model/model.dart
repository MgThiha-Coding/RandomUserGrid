class Users{
  // name / gender / email / phone / country //
   String name;
   String gender;
   String email;
   String phone;
   String country;
   String image;
   Users({
     required this.name,
     required this.gender,
     required this.email,
     required this.phone,
     required this.country,
     required this.image,
   });
}